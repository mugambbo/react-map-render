import React from 'react'

const Loading = () => {
    return (
        <div style={{color: 'grey', display: 'flex', alignItems: 'center', justifyContent: 'center'}}>Loading</div>
    )
}

export default Loading;